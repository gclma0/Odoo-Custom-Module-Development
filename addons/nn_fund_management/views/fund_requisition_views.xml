<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_nn_fund_requisition_tree" model="ir.ui.view">
        <field name="name">nn.fund.requisition.tree</field>
        <field name="model">nn.fund.requisition</field>
        <field name="arch" type="xml">
            <tree string="Fund Requisitions">
                <field name="requisition_number" />
                <field name="request_date" />
                <field name="required_date" />
                <field name="project_id" optional="show" />
                <field name="expense_head_id" optional="show" />
                <field name="requested_by" />
                <field name="amount" />
                <field name="remaining_billable_amount" />
                <field name="state" widget="badge" decoration-warning="state in ('submitted', 'gm_approval', 'finance_approval', 'md_approval')" decoration-success="state in ('approved', 'closed')" decoration-danger="state in ('rejected', 'cancelled', 'reversed')" />
            </tree>
        </field>
    </record>

    <record id="view_nn_fund_requisition_form" model="ir.ui.view">
        <field name="name">nn.fund.requisition.form</field>
        <field name="model">nn.fund.requisition</field>
        <field name="arch" type="xml">
            <form string="Fund Requisition">
                <header>
                    <button name="action_submit" type="object" string="Submit" class="btn-primary" invisible="state != 'draft'" />
                    <button name="action_approve" type="object" string="Approve" class="btn-primary" invisible="state not in ('submitted', 'gm_approval', 'finance_approval', 'md_approval')" groups="nn_fund_management.group_finance_user,nn_fund_management.group_gm_approver,nn_fund_management.group_md_approver,nn_fund_management.group_fund_administrator" />
                    <button name="action_reject" type="object" string="Reject" invisible="state not in ('submitted', 'gm_approval', 'finance_approval', 'md_approval')" groups="nn_fund_management.group_finance_user,nn_fund_management.group_gm_approver,nn_fund_management.group_md_approver,nn_fund_management.group_fund_administrator" />
                    <button name="action_reverse" type="object" string="Reverse" invisible="state != 'approved'" groups="nn_fund_management.group_finance_user,nn_fund_management.group_fund_administrator" />
                    <button name="action_cancel" type="object" string="Cancel" invisible="state in ('approved', 'closed', 'cancelled')" />
                    <button name="action_close" type="object" string="Close" invisible="state != 'approved'" groups="nn_fund_management.group_finance_user,nn_fund_management.group_fund_administrator" />
                    <button name="action_reset_to_draft" type="object" string="Reset to Draft" invisible="state not in ('rejected', 'cancelled')" />
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,gm_approval,finance_approval,md_approval,approved,closed,rejected,cancelled,reversed" />
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="requisition_number" readonly="1" />
                            <field name="request_date" readonly="state != 'draft'" />
                            <field name="required_date" readonly="state != 'draft'" />
                            <field name="amount" readonly="state != 'draft'" />
                        </group>
                        <group>
                            <field name="requested_by" readonly="1" />
                            <field name="company_id" groups="base.group_multi_company" readonly="1" />
                            <field name="currency_id" readonly="1" />
                            <field name="remaining_billable_amount" readonly="1" />
                        </group>
                    </group>
                    <group string="Requisition Source">
                        <field name="project_id" readonly="state != 'draft'" invisible="expense_head_id" />
                        <field name="expense_head_id" readonly="state != 'draft'" invisible="project_id" />
                    </group>
                    <group string="Purpose and Attachment">
                        <field name="purpose" readonly="state != 'draft'" placeholder="Explain why this requisition is needed." />
                        <field name="attachment_filename" invisible="1" />
                        <field name="supporting_attachment" filename="attachment_filename" readonly="state != 'draft'" />
                    </group>
                    <group string="Approval">
                        <field name="approval_config_id" readonly="1" invisible="not approval_config_id" />
                        <field name="current_approval_line_id" readonly="1" invisible="state in ('draft', 'approved', 'closed', 'rejected', 'cancelled')" />
                        <field name="approval_comment" placeholder="Optional approval or rejection comment." />
                    </group>
                    <group string="Billing Reserve">
                        <field name="spent_amount" readonly="1" />
                        <field name="released_amount" readonly="1" />
                    </group>
                    <notebook>
                        <page string="Approval History">
                            <field name="approval_history_ids" readonly="1">
                                <tree create="0" edit="0" delete="0">
                                    <field name="action_date" />
                                    <field name="approval_level" />
                                    <field name="decision" />
                                    <field name="action_by" />
                                    <field name="old_state" />
                                    <field name="new_state" />
                                    <field name="comment" />
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="view_nn_fund_requisition_search" model="ir.ui.view">
        <field name="name">nn.fund.requisition.search</field>
        <field name="model">nn.fund.requisition</field>
        <field name="arch" type="xml">
            <search string="Fund Requisitions">
                <field name="requisition_number" />
                <field name="project_id" />
                <field name="expense_head_id" />
                <field name="requested_by" />
                <field name="state" />
                <filter name="filter_pending" string="Pending Approval" domain="[('state', 'in', ('submitted', 'gm_approval', 'finance_approval', 'md_approval'))]" />
                <filter name="filter_approved" string="Approved" domain="[('state', '=', 'approved')]" />
                <filter name="filter_closed" string="Closed" domain="[('state', '=', 'closed')]" />
                <filter name="filter_reversed" string="Reversed" domain="[('state', '=', 'reversed')]" />
                <group expand="0" string="Group By">
                    <filter name="group_by_state" string="Status" context="{'group_by': 'state'}" />
                    <filter name="group_by_requester" string="Requested By" context="{'group_by': 'requested_by'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="action_nn_fund_requisition" model="ir.actions.act_window">
        <field name="name">Fund Requisitions</field>
        <field name="res_model">nn.fund.requisition</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_nn_fund_requisition_search" />
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create your first fund requisition</p>
            <p>
                Requisitions reserve approved project or expense funds so they can later be consumed by bills without double spending.
            </p>
        </field>
    </record>
</odoo>
