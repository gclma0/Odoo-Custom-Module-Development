<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_nn_fund_transfer_tree" model="ir.ui.view">
        <field name="name">nn.fund.transfer.tree</field>
        <field name="model">nn.fund.transfer</field>
        <field name="arch" type="xml">
            <tree string="Fund Transfers">
                <field name="transfer_number" />
                <field name="request_date" />
                <field name="source_name" />
                <field name="destination_name" />
                <field name="requested_by" />
                <field name="amount" />
                <field name="state" widget="badge" decoration-warning="state in ('submitted', 'gm_approval', 'finance_approval', 'md_approval')" decoration-success="state == 'approved'" decoration-danger="state in ('rejected', 'cancelled')" />
            </tree>
        </field>
    </record>

    <record id="view_nn_fund_transfer_form" model="ir.ui.view">
        <field name="name">nn.fund.transfer.form</field>
        <field name="model">nn.fund.transfer</field>
        <field name="arch" type="xml">
            <form string="Fund Transfer">
                <header>
                    <button name="action_submit" type="object" string="Submit" class="btn-primary" invisible="state != 'draft'" />
                    <button name="action_approve" type="object" string="Approve" class="btn-primary" invisible="state not in ('submitted', 'gm_approval', 'finance_approval', 'md_approval')" groups="nn_fund_management.group_finance_user,nn_fund_management.group_gm_approver,nn_fund_management.group_md_approver,nn_fund_management.group_fund_administrator" />
                    <button name="action_reject" type="object" string="Reject" invisible="state not in ('submitted', 'gm_approval', 'finance_approval', 'md_approval')" groups="nn_fund_management.group_finance_user,nn_fund_management.group_gm_approver,nn_fund_management.group_md_approver,nn_fund_management.group_fund_administrator" />
                    <button name="action_cancel" type="object" string="Cancel" invisible="state in ('approved', 'cancelled')" />
                    <button name="action_reset_to_draft" type="object" string="Reset to Draft" invisible="state not in ('rejected', 'cancelled')" />
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,gm_approval,finance_approval,md_approval,approved,rejected,cancelled" />
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="transfer_number" readonly="1" />
                            <field name="request_date" readonly="state != 'draft'" />
                            <field name="amount" readonly="state != 'draft'" />
                            <field name="requested_by" readonly="1" />
                        </group>
                        <group>
                            <field name="company_id" groups="base.group_multi_company" readonly="1" />
                            <field name="currency_id" readonly="1" />
                            <field name="approval_config_id" readonly="1" invisible="not approval_config_id" />
                            <field name="current_approval_line_id" readonly="1" invisible="state in ('draft', 'approved', 'rejected', 'cancelled')" />
                        </group>
                    </group>
                    <group string="Source">
                        <field name="source_project_id" readonly="state != 'draft'" invisible="source_expense_head_id" />
                        <field name="source_expense_head_id" readonly="state != 'draft'" invisible="source_project_id" />
                    </group>
                    <group string="Destination">
                        <field name="destination_project_id" readonly="state != 'draft'" invisible="destination_expense_head_id" />
                        <field name="destination_expense_head_id" readonly="state != 'draft'" invisible="destination_project_id" />
                    </group>
                    <group string="Reason">
                        <field name="reason" readonly="state != 'draft'" placeholder="Explain the reason for this transfer." />
                        <field name="approval_comment" placeholder="Optional approval or rejection comment." />
                    </group>
                    <notebook>
                        <page string="Approval History">
                            <field name="approval_history_ids" readonly="1">
                                <tree create="0" edit="0" delete="0">
                                    <field name="action_date" />
                                    <field name="approval_level" />
                                    <field name="decision" />
                                    <field name="action_by" />
                                    <field name="old_state" />
                                    <field name="new_state" />
                                    <field name="comment" />
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="view_nn_fund_transfer_search" model="ir.ui.view">
        <field name="name">nn.fund.transfer.search</field>
        <field name="model">nn.fund.transfer</field>
        <field name="arch" type="xml">
            <search string="Fund Transfers">
                <field name="transfer_number" />
                <field name="requested_by" />
                <field name="state" />
                <filter name="filter_pending" string="Pending Approval" domain="[('state', 'in', ('submitted', 'gm_approval', 'finance_approval', 'md_approval'))]" />
                <filter name="filter_approved" string="Approved" domain="[('state', '=', 'approved')]" />
                <group expand="0" string="Group By">
                    <filter name="group_by_state" string="Status" context="{'group_by': 'state'}" />
                    <filter name="group_by_requester" string="Requested By" context="{'group_by': 'requested_by'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="action_nn_fund_transfer" model="ir.actions.act_window">
        <field name="name">Fund Transfers</field>
        <field name="res_model">nn.fund.transfer</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_nn_fund_transfer_search" />
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create your first fund transfer</p>
            <p>
                Transfer available balances between projects and expense heads. Pending transfers keep the amount on hold until approval is completed.
            </p>
        </field>
    </record>
</odoo>
